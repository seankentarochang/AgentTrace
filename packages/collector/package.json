{
  "name": "@agenttrace/collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/server.ts",
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "check": "tsx src/check.ts && tsx src/check-review.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agenttrace/protocol": "*",
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.0",
    "tsx": "^4.20.3",
    "typescript": "^5.6.3"
  }
}
